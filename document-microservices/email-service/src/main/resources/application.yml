spring:
  application:
    name: email-service
  mail:
    host: smtp.gmail.com
    port: 587
    username: madhuchary21@gmail.com
    password: Madhu@1245
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost

server:
  port: 8002

logging:
  level:
    root: INFO
    com.example.email: DEBUG
